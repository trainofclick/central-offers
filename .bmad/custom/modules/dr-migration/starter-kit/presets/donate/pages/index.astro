---
/**
 * Landing Page - DONATE Preset
 * VSL ou copy de vendas com CTA para checkout
 */
import BaseLayout from "@layouts/BaseLayout.astro";
import Section from "@components/astro/Section.astro";
import Button from "@components/astro/Button.astro";
import Card from "@components/astro/Card.astro";
import Timer from "@components/react/Timer";
import { fbEvents } from "@lib/tracking";

// Track ViewContent on page load
const pageTitle = "Página Principal";
const pageDescription = "Descrição do seu produto ou causa";
---

<BaseLayout title={pageTitle} description={pageDescription}>
  <!-- Hero Section -->
  <Section background="gradient" padding="xl">
    <div class="text-center max-w-4xl mx-auto">
      <!-- Badge -->
      <div
        class="inline-block px-4 py-1 bg-primary/10 text-primary rounded-full text-sm font-medium mb-6"
      >
        Oferta por tempo limitado
      </div>

      <!-- Headline -->
      <h1 class="text-4xl md:text-5xl lg:text-6xl font-extrabold text-secondary mb-6">
        <!-- TODO: Customize headline -->
        Seu Título Principal Aqui
        <span class="gradient-text">Com Destaque</span>
      </h1>

      <!-- Subheadline -->
      <p class="text-xl text-text-muted mb-8 max-w-2xl mx-auto">
        <!-- TODO: Customize subheadline -->
        Sua descrição convincente que explica o valor da sua oferta e por que o
        visitante deve agir agora.
      </p>

      <!-- Timer -->
      <div class="mb-8">
        <Timer client:load initialMinutes={15} prefix="Esta oferta expira em:" />
      </div>

      <!-- CTA Button -->
      <Button
        href="/checkout"
        variant="success"
        size="xl"
        class="animate-pulse-slow"
        data-event="cta_click"
      >
        QUERO CONTRIBUIR AGORA
      </Button>

      <!-- Trust indicators -->
      <p class="mt-4 text-sm text-text-muted">
        Pagamento 100% seguro via PIX
      </p>
    </div>
  </Section>

  <!-- Video Section (optional) -->
  <Section id="video" background="surface" padding="lg">
    <div class="max-w-4xl mx-auto">
      <div class="aspect-video bg-secondary/10 rounded-xl flex items-center justify-center">
        <!-- TODO: Add your video embed -->
        <p class="text-text-muted">Seu vídeo aqui (VSL ou apresentação)</p>
      </div>
    </div>
  </Section>

  <!-- Benefits Section -->
  <Section id="beneficios" padding="lg">
    <div class="text-center mb-12">
      <h2 class="text-3xl font-bold text-secondary mb-4">
        Por Que Contribuir?
      </h2>
      <p class="text-text-muted max-w-2xl mx-auto">
        <!-- TODO: Customize -->
        Veja o que você ganha ao fazer parte dessa causa
      </p>
    </div>

    <div class="grid md:grid-cols-3 gap-6">
      {
        [
          {
            icon: "🎯",
            title: "Benefício 1",
            desc: "Descrição do primeiro benefício",
          },
          {
            icon: "💡",
            title: "Benefício 2",
            desc: "Descrição do segundo benefício",
          },
          {
            icon: "🚀",
            title: "Benefício 3",
            desc: "Descrição do terceiro benefício",
          },
        ].map((item) => (
          <Card variant="elevated" class="text-center">
            <div class="text-4xl mb-4">{item.icon}</div>
            <h3 class="text-xl font-bold text-secondary mb-2">{item.title}</h3>
            <p class="text-text-muted">{item.desc}</p>
          </Card>
        ))
      }
    </div>
  </Section>

  <!-- Testimonials Section -->
  <Section id="depoimentos" background="surface" padding="lg">
    <div class="text-center mb-12">
      <h2 class="text-3xl font-bold text-secondary mb-4">
        O Que Dizem Sobre Nós
      </h2>
    </div>

    <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-6">
      {
        [1, 2, 3].map((i) => (
          <Card>
            <div class="flex items-start gap-4">
              <div class="w-12 h-12 bg-primary/20 rounded-full flex items-center justify-center text-primary font-bold">
                {i}
              </div>
              <div>
                <p class="text-text-muted italic mb-2">
                  "Depoimento do cliente {i} aqui. Conte sua experiência positiva."
                </p>
                <p class="font-semibold text-secondary">Nome do Cliente</p>
              </div>
            </div>
          </Card>
        ))
      }
    </div>
  </Section>

  <!-- Final CTA Section -->
  <Section background="dark" padding="xl">
    <div class="text-center max-w-3xl mx-auto">
      <h2 class="text-3xl md:text-4xl font-bold text-white mb-6">
        Pronto Para Fazer Parte?
      </h2>
      <p class="text-gray-300 mb-8">
        Sua contribuição faz toda a diferença. Escolha um valor e contribua agora
        mesmo via PIX.
      </p>

      <Button href="/checkout" variant="success" size="xl">
        CONTRIBUIR AGORA
      </Button>

      <p class="mt-4 text-sm text-gray-400">
        PIX instantâneo e 100% seguro
      </p>
    </div>
  </Section>

  <!-- Track ViewContent -->
  <script>
    import { fbEvents } from "@lib/tracking";
    fbEvents.viewContent({
      contentName: "Landing Page",
      contentCategory: "DONATE",
    });
  </script>
</BaseLayout>
